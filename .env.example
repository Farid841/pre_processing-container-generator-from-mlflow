# Kafka Configuration
# Enable Kafka output (set to "true" to enable, "false" or leave empty for stdout)
KAFKA_ENABLED=true

# Kafka bootstrap servers
# For local access from host machine:
KAFKA_BOOTSTRAP_SERVERS=localhost:29092
# For access from Docker container in same network:
# KAFKA_BOOTSTRAP_SERVERS=kafka:9092

# Kafka topic suffix (will be appended to model-version)
# Examples: "cleaned", "pre-processed", "processed"
KAFKA_TOPIC_SUFFIX=cleaned

# Kafka security protocol (PLAINTEXT, SASL_PLAINTEXT, SASL_SSL, SSL)
KAFKA_SECURITY_PROTOCOL=PLAINTEXT

# SASL Configuration (only needed if KAFKA_SECURITY_PROTOCOL is SASL_PLAINTEXT or SASL_SSL)
# KAFKA_SASL_MECHANISM=PLAIN
# KAFKA_SASL_USERNAME=your_username
# KAFKA_SASL_PASSWORD=your_password

# SSL Configuration (only needed if KAFKA_SECURITY_PROTOCOL is SSL or SASL_SSL)
# KAFKA_SSL_CA_LOCATION=/path/to/ca-cert
# KAFKA_SSL_CERT_LOCATION=/path/to/client-cert
# KAFKA_SSL_KEY_LOCATION=/path/to/client-key

# Model Information (normally set by build script, but can be overridden)
# These are automatically set from MLflow during build, but you can override them here
# MODEL_NAME=model
# MODEL_VERSION=v1
# COMPONENT_TYPE=preprocessing
